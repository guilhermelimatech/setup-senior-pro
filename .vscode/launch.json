{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Node: current file (tsx)",
      "type": "node",
      "request": "launch",
      "program": "${file}",
      "runtimeExecutable": "tsx",
      "cwd": "${workspaceFolder}"
    }
  ]
}
